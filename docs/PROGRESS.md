# Progress Log

## Current Status
- Phase: 1 (MVP — "The Glitch")
- Active milestone: Vertical slice — first real content built (objective loop working end-to-end), art/theme not started
- Gate status: §10a and §10b both passed. Designer handed over full theme direction (see the_glitch_table_layout.md/the_glitch_rendering_notes.md) and gave full autonomy on sequencing, with an explicit preference for well-designed and well-tested over fast. Started the vertical slice with structure first: core/table_base + core/objectives (data-driven, per GDD §11 lock) before any table-specific content, then built the first real objective (Shader Node Targets → Shader Rebuild) on top. Not yet built: The Glitch Core toy, neon flipper/slingshot reskin, any real art.

## Session Log
- 2026-07-15 (cont. 38): The deflector-plate fix from the last increment turned out to be the wrong approach -- designer sent a screenshot after live-testing it, and the gray deflector plate was visibly cutting straight across the magenta RightOrbit loop, physically colliding with it. Designer clarified the actual intended design: the ball exiting the shooter lane was never supposed to be walled off with new geometry -- it was supposed to pass *under* RightOrbit's rail, the same way CyberRampA and CyberRampB already pass over/under each other via physics-layer masking. The "crashing into a wall" complaint from the previous report was this, not a residual shooter-lane bug.
  Removed the deflector plate entirely. Gave RightOrbit its own dedicated physics layer (project.godot: layer_4 = "right_orbit") instead of sharing the default layer everything else uses, so a ball at its normal default collision mask no longer overlaps it at all -- passes straight through/under, exactly the CyberRampA/B technique applied to this rail. Deliberately did NOT add an entrance/exit trigger pair to let a properly-aimed flipper shot actually ride the loop as a real orbit shot -- that's a real, separate follow-up feature (which mouth is the entrance, which direction counts as a made shot) that needs the designer's input, not something to guess at while fixing a collision bug. RightOrbit is pass-through for every ball for now, not just shooter-lane ones.
  While verifying via headless trace (freeing the deflector let the ball fall back through the whole table for the first time), surfaced a real pre-existing bug: saucer.gd was setting `body.freeze = true` directly inside `_on_body_entered`, which is illegal while Godot's physics engine is mid-flush on collision queries -- silent before now because the ball had never fallen back through open space fast/far enough to trigger it. Fixed by deferring that one assignment; updated its test to await a physics frame before asserting frozen state, since the timing is now genuinely different by design, not a workaround.
  94/94 tests pass, duplicate-index audit clean, verified via headless trace: the ball passes under RightOrbit with continuous velocity and no anomalous jumps, and the saucer error is gone. The lane-widening fix from the previous increment stays -- that was a separate, real bug (lane narrower than the ball) unrelated to this one.
- 2026-07-15 (cont. 37): Designer reported the crossing-orbit fix wasn't landing as a clean fix -- balls were still crashing at "the same level" up there and the only way to reach that area at all was via the shooter lane. Tested Godot's layer/mask collision rule directly (isolated clean-room test) and confirmed the masking mechanism itself is sound; the real explanation was that the shooter lane's own bug was still the only path feeding that area, so any ball arriving there was arriving un-aimed and chaotically, not via a proper entrance trigger -- explaining why it looked like a new problem when it was fallout from the same old one. Designer explicitly signed off on digging into the shooter lane properly rather than working around it, since nothing else can be tested until the ball can reliably leave the lane.
  Found the actual root cause on this pass, and it wasn't the top-of-lane corner at all: measured the exact numbers and the shooter lane corridor (between ShooterLaneWall's right face at x=425 and RightWall's left face at x=440) is only 15 units wide, while the ball is 28 units in diameter. The ball's own spawn point (x=435) was already overlapping *both* walls simultaneously before it ever moved -- it had been wedged fighting double-penetration against both sides of its own lane for its entire trip up, every single time, which explains why every previous deflector attempt (this session and last) produced such noisy, inconsistent results: the ball was never in a clean physical state to begin with, regardless of what was waiting at the top.
  Fixed in two parts. First, widened the lane itself (core/physics_prototype.tscn) to 34 units by moving ShooterLaneWall left, and re-centered the ball's spawn point and the table's plunger visuals to match -- verified via headless trace that the ball's horizontal position is now rock-solid through the whole climb, instead of the slow drift observed before. Second, with the ball's physics finally clean, retried a deflector plate at the top (a single low-friction angled plate, informed by the ball's actual bounce=0.6/friction=0.1 material) and this time it worked: verified across the realistic charge range that weak shots harmlessly fall back for another try, and strong shots (roughly 90%+ charge) now land clearly out in open playfield space instead of settling back into the corner pocket every earlier attempt got stuck in. 94/94 tests pass, duplicate-index audit clean, and both the isolated probe and the actual wired-in scene were verified to produce matching results before committing.
- 2026-07-15 (cont. 36): Designer reported live-test feedback on the art swap: flippers/bumpers/redesign look good, but the ball still wasn't getting out of the shooter lane, and was "bumping and crashing" instead of passing through the crossing orbits on the top floor. Investigated the crossing-orbit report first since it was concretely diagnosable: CyberRampA/B and LeftOrbit/RightOrbit were each built from 16-20 separately-rotated RectangleShape2D segments chained together to approximate a curve. Computed the exact endpoint math for every joint -- adjacent segments only met within ~0.7-1.1 units of each other, and every segment's flat 90-degree end cap sticks a physical lip into the channel regardless. A ball crossing between segments catches on these lips, which is exactly the reported "bumping and crashing." Fixed by replacing all 4 rails (X-ramp inner/outer x2) and both orbit loops (inner/outer x2) with a single ConcavePolygonShape2D per rail, built from the exact same tuned centerline points and angles the old segments used -- curve shape unchanged, only the collision representation is now seamless. Collapsed ~930 lines / 144 nodes down to ~210 lines / 32 nodes. Verified via a headless ball-trace through the reworked CyberRampA climb: velocity magnitude now decreases continuously with zero anomalous jumps, versus visible bump artifacts before the fix. 94/94 tests still pass, duplicate-sibling-index audit clean across all 81 parent groups.
  Then spent substantial additional effort on the shooter-lane "ball doesn't get out" bug (previously left unresolved after 5 failed attempts last session). Made real diagnostic progress beyond last time: traced the ball's actual max-charge trajectory and confirmed it reaches the top of ShooterLaneWall (y=-1250) at high speed moving almost perfectly vertically, clips the wall's flat top-right corner, flies up to hit the TopWall ceiling, bounces back down, and settles rattling right at that corner -- never crossing cleanly into the open playfield. Discovered a genuine physics-material insight along the way: the ball's own script sets restitution=0.6/friction=0.1, which matters a lot for how any deflector should be angled (confirmed via an isolated clean-room test that an angled low-friction plate CAN redirect it well in principle). Despite that, every deflector geometry tried against the real scene -- angled plates at multiple angles/positions, a tangent-matched smooth arc continuous with the wall's own corner -- produced some initial leftward kick but then the ball consistently settled back into a stable pocket around x=415-426, y=-1260 to -1310 formed between the wall's corner and whatever sits above it, rather than clearing into the field. Concluded this specific spot needs live/visual iteration (watching actual collision contacts with Visible Collision Shapes in the editor) rather than more headless geometry guessing, and left it untouched rather than shipping another unverified attempt -- consistent with this project's established discipline of reverting cleanly when a fix isn't proven. Committed the crossing-orbit fix on its own (`2b874fa`), cleaned up all scratch probe scripts before committing, and reported both the fix and the honest state of the unresolved shooter-lane issue back to the designer.
- 2026-07-15 (cont. 35): Major decision, made carefully rather than reactively: designer leaned toward a full 3D pivot after today's shooter-lane struggles and general visual-quality anxiety. Pushed back with real analysis before any action -- corrected a real misconception (3D wouldn't have fixed the shooter-lane bug specifically; that's a generic rigid-body collision-tuning problem in any engine) and quantified the actual rewrite cost (~17 physics scripts, every scene built this session, all 94 tests) against the GDD's deliberately locked 2D decision. Also confirmed directly: the "ball runs free under a ramp, or climbs into it if shot right" mechanic the designer specifically wanted does NOT require 3D -- it's exactly what ramp_layer_zone.gd (built earlier this session) already does via physics-layer masking, a standard 2D pinball technique. Landed on a middle path instead: keep all 2D physics/gameplay, pursue real visual quality via "2.5D" -- model and light pieces once in Blender, bake to flat textures, use as sprites in the existing 2D game.
  Installed Blender (apt, headless via `blender --background --python`), built a fully scripted render pipeline (bmesh profile extrusion reusing each element's own in-game collision/visual point lists, so 3D silhouettes match tuned 2D shapes exactly; 3-point + rim lighting; orthographic camera matching the game's projection; transparent PNG output) -- zero manual GUI modeling. Validated on a pop bumper first, then built 7 more test assets (flipper x2 colors, slingshot x2 colors, wall segment, standup target, GlitchCore, saucer, ball), catching and fixing real issues along the way: flat/large-surface objects (walls, slingshots) blow out badly under lighting tuned for curved objects (pop bumper's dome only catches small highlights) -- needed separate, lower light levels per shape type; a pure metallic=1.0 ball with no environment to reflect renders as an almost-black void -- needed softened metallic/roughness plus an actual (transparent-output-safe) world background for it to reflect; camera framing broke on off-center profiles (the flipper's pivot isn't at its visual center) -- fixed by explicitly centering each object's geometry before framing rather than assuming origin = center. The saucer was deliberately built as a genuine sunken hole (dark inset well + glowing collar ring) rather than another raised bump, to test whether the technique could convey that real distinction -- it did, convincingly.
  Swapped 6 of the 8 validated assets into the live game (ball, both flipper colors, pop bumper x3, saucer x5, slingshot x2 colors, standup target x8) -- deliberately held back GlitchCore (its charged/uncharged color swap is a real gameplay-state signal a static baked texture would silently break -- needs a neutral/white-based texture designed for runtime modulate-tinting, not a rushed swap) and the wall segments (aspect-ratio/tiling mismatch against the actual long wall pieces, needs a proper tiling approach). Caught and fixed a real architecture violation mid-edit: initially referenced tables/the_glitch/art/ textures directly from the shared core/physics_prototype.tscn, which would have made core/ depend on tables/ -- moved the genuinely universal assets (ball, flippers -- the cyan/magenta palette is already shared across the whole table, not Glitch-specific) to a new core/physics/art/ instead. Refactored standup_target.gd and slingshot.gd's flash/tint logic from Polygon2D-only `.color` to CanvasItem-universal `.modulate` (works on both the old flat shapes and the new textured sprites, and is a genuine simplification -- no more remembered _base_color state). Verified thoroughly before committing: 94/94 tests, clean headless parse and real runtime execution on both scenes, a full duplicate-index audit (zero issues across 81 parent groups), and an actual rendered screenshot of the live scene confirming correct position/scale/orientation for every swapped element -- not just "no errors."
- 2026-07-15 (cont. 34): With the camera fix, designer could finally see the shooter lane clearly and reported SkillShotLane itself blocking the exit -- checked and confirmed exactly right: it sat at x=404-412, just 3 units from the partition wall's edge (415-425), the two together forming one continuous barrier neither Cowork nor I had accounted for in four prior fix attempts. Moved it to a short sliver at the very top (y=-1340/-1400), fully clear of the merge zone.
  Made a fifth attempt at the core stuck-ball problem, this time deliberately mirroring RightGuide's proven shallow-slope technique (gravity-rolled, not a high-speed bounce -- every prior attempt used the latter and failed). Got real measurable progress for the first time (ball moved from a completely static x=426 to x=418.6), but extending and steepening the slope twice more couldn't get it further -- it kept settling in the same narrow x=418-426 band regardless of slope geometry, pointing at a sharp-corner contact resolution issue at the wall/slope join that position-logging can't diagnose. Reverted cleanly (confirmed zero diff against the committed baseline) rather than ship a fifth unverified change; kept only the independently-useful SkillShotLane fix. 94/94 tests passing.
  Also clarified for the designer that the plunger's UP-arrow control needs to be held, not tapped (charge-based since earlier this session), and confirmed the mini-flipper anchor-bolt report was very likely a byproduct of the camera bug -- everything was rendered too small to judge properly before this fix.
- 2026-07-15 (cont. 33): Designer's own live report surfaced a real foundational bug spanning the whole session: "the image is so small, camera should follow the ball." Checked the actual numbers -- viewport_height (2100) exactly equalled the total table height (top -1400 to bottom 700), confirming the entire table has been rendered in one static frame this whole time with zero scrolling, at whatever tiny scale fit it all. This traces back to every height extension always resizing the viewport to match the new total height, which made sense at the original scale but silently broke down as the table grew ~4x taller across the session. Likely explains some of the earlier mini-flipper "looks broken" confusion too -- hard to judge anything at that render scale.
  Fixed properly: viewport_height back to a real single-screen size (1050), added core/physics/ball_follow_camera.gd (Camera2D now scrolls vertically to track the ball, clamped to the actual wall positions so it never shows past them, horizontal position stays fixed since table width already matches viewport width). Touch zones reverted to their fixed-screen-space convention (985, matching the visible 1050-tall screen, not the old 2100 world-scroll-range value) -- Feedback UI needed no changes, already anchored from an earlier fix. Verified empirically via a real simulated launch, not just parse-checked -- camera correctly tracks the climb and clamps at the top wall on arrival. 94/94 tests passing.
  Also clarified for the designer: launch_ball is the UP arrow (confirmed via Godot's actual keycode values, not guessed), and per the charge-based plunger built earlier this session it must be held, not tapped -- likely explains "I can't shoot it" if the old instant-tap habit carried over.
- 2026-07-15 (cont. 32): Cowork could code-review the Level 2 wiring cleanly (confirmed _award() correctly doubles exactly the four intended targets, gated behind _level2_active, both feedback messages present) but had no way to verify it live -- no method-invoke tool, and the shooter lane still being stuck meant normal play can't reach any of this content either. Added 4 more debug teleport keys (4/5/6/7: VUK-to-Level-2, rollunder gate, cyber ramp A entrance, captive ball 1) alongside the existing 1/2/3, giving full reachability to every piece of the Level 2 path independent of the shooter lane fix. 91/91 tests passing.
- 2026-07-15 (cont. 31): Attempted the shooter-lane merge fix Cowork flagged (ball reaches the top of the lane but rests in a dead corner instead of entering the main playfield -- confirmed by Cowork with exact numbers: (426,-1264), a genuine flat-ledge stable equilibrium, not a settling-in-progress issue). Made four different geometric attempts via headless simulation -- a steep single deflector (redirected ~13 units, not enough), a shallow deflector (caused the ball to tunnel through the TopWall entirely at high impulse -- a new bug, reverted), a 6-segment curved cap (accidentally positioned at the wall's own x-coordinate rather than out in the lane where the ball actually travels, never got touched), and a corrected single straight ramp in the ball's actual path (empirically made things measurably worse -- ball fell back past its own spawn point instead of reaching the corner). Reverted cleanly to the last known-good, Cowork-verified state (confirmed zero diff against the committed version) rather than ship any of the four failed attempts. Concluded, with real repeated evidence now rather than a hunch, that this specific fix needs live visual iteration (watching the actual collision in the editor) rather than more headless trajectory-log guessing, which has a demonstrated track record of not converging here.
  Designer said keep building, defer that fix to later. Picked something deliberately safe given the day's geometry track record: resolved the "Level 2" open design question from docs/design/the_glitch_full_layout_plan.md (real separate physical zone vs. a state/scoring layer) by choosing the scoring-layer approach -- zero new collision geometry, zero risk. VUK-to-Level-2 capture now starts a 15-second "Level 2 Active" window during which rollunder gate, both cyber ramp entrances, and captive ball strikes score double, via a new `_award()` helper rather than a blanket multiplier over every score call on the table. 91/91 tests passing, verified via actual scene execution.
- 2026-07-15 (cont. 30): Cowork's first playtest pass on the rebuilt table found a real blocker immediately: launch_impulse_max=950 (leftover from before this session's table-height extensions, never retuned) couldn't get the ball anywhere close to clearing the new 1900-unit shooter lane, making the entire new TopZone unreachable. Correctly diagnosed by Cowork as an order-of-magnitude scaling mismatch.
  Calibrated the fix empirically rather than hand-deriving a new constant (which would likely have been wrong again): checked the real project physics settings first (gravity=980, a real 0.1 default linear_damp a naive frictionless estimate would have missed), then used a headless probe firing a range of impulses at the actual ball setup and measuring real peak height. 2000 fell short, 2200 cleared, set launch_impulse_max=2600 for margin. Verified via full-scene simulation that this reliably reaches the top without the ball tunneling out of the table (confirmed tunneling only becomes possible around 4000+, safely above the new max).
  While verifying, found a second, separate problem beyond what Cowork reported: even after reaching the top of the lane, the ball settles in a corner at the lane/playfield boundary rather than rolling into TopZone. Tried two deflector geometries to fix it -- neither worked reliably, and the second one caused the ball to tunnel through the TopWall entirely at high impulse (a new bug, not a fix). Removed the deflector rather than ship something unreliable, and reported this honestly as unresolved rather than overclaiming the plunger fix solved everything. Needs a proper curved multi-segment merge (the same technique already proven for the orbit loops), built with visual iteration rather than more blind headless numeric guessing. 91/91 tests passing.
- 2026-07-15 (cont. 29): Designer flagged two real gaps and asked me to zoom into the reference image closely before touching anything -- good call, it surfaced a genuinely useful insight: the reference's base material is neutral brushed metal with color reserved for glow accents, not flat full-saturation fills everywhere (our actual gap wasn't really the flippers/slingshots, which are already close -- it was the background/base tone). Shifted bg_color from near-black to a neutral charcoal, a cheap change that doesn't require the full art pipeline.
  Bigger fix: there was no real shooter lane at all -- ball spawned in open space, launch was a raw instant impulse, pure leftover from the very first §10a prototype. Rather than squeeze a lane into the already-fragile right-edge corridor (which took multiple rounds to get right earlier this session), widened the shared table (420->450) to open genuinely new space, added a partition wall separating it from the main playfield, and moved the ball's spawn into it. Built a real charge-and-release plunger (hold to pull back, release fires proportional to charge) replacing the instant-tap placeholder, with a visual rod that animates from the new launch_charge_changed signal. 91/91 tests passing, verified via actual scene execution (not just parse-checking) since the width change touched camera centering and wall geometry broadly.
- 2026-07-15 (cont. 28): Designer asked for the entire table rebuilt to match the AI-generated concept image in full, explicitly accepting the "single unverified pass, proper debugging at the end" risk after I flagged it clearly (per CLAUDE.md's standing instruction to be honest about risk rather than default to reassurance) -- captive balls confirmed back in scope, contradicting an earlier explicit descoping a few messages prior in the same session, which the designer confirmed was an intentional change of mind, not an oversight.
  Documented the real architecture first (docs/design/the_glitch_full_layout_plan.md): zone breakdown, node hierarchy, curved-collision geometry approach, 6 new core primitives needed, dependency-ordered build sequence. Then built all of it:
  1. **5 new core physics primitives** (mini-flipper needed no new script -- flipper.gd reused with smaller-scale export tuning, confirmed reusable as planned): magnet.gd, timed_gate.gd, collapsing_bridge.gd, subway_tunnel.gd (built on ball.gd's existing request_teleport() rather than reimplementing teleportation -- directly reuses the CCD-disable fix from earlier this session instead of risking the same bug again), captive_ball.gd. Each with GUT coverage.
  2. **ramp_layer_zone.gd** -- the mechanism letting two ramps physically cross in flat 2D without colliding at the crossing point: each ramp's walls on their own dedicated physics layer (added cyber_ramp_a/b as named layers 2/3), ball's collision_mask switched by entrance/exit trigger areas. Documented limitation: outside a ramp's own entrance influence, its walls are inert to any ball that didn't enter through the defined mouth -- a deliberate, flagged simplification, not an oversight.
  3. Extended the table another 750px (top now y=-1400, was -650) using the same proven method as the two earlier extensions this session -- all existing (already-tuned, already-playtested) content completely untouched.
  4. Built the full UpperZone in three sub-zones: MidZone (timed gate, 3 mini targets, mini-flipper, VUK-to-Level-2, mini saucer kickout, collapsing bridge, subway), CrossoverZone (rollunder gate, 2 orbit magnets, multi-level gate, the X-crossing cyber ramps -- curved channel walls computed from a quadratic Bezier via script rather than hand-placed points, verified segment-length math before writing coordinates), TopZone (dual orbit loops via ellipse-arch parametrization, capsule lock, magnetic accelerator trap, 2 captive balls, skill-shot lane + target).
  5. Wired every new element to scoring/objectives -- two new hit_targets objectives (mini_target_cluster, captive_dual), and cross-wired old content to new (standup bank completion now also opens the new timed gate; rollunder gate opens the multi-level gate; VUK-to-Level-2 activates the magnetic trap) rather than treating UpperZone as isolated.
  Caught and fixed two real bugs via systematic verification, not playtesting: (a) an index-collision bug in my own generated X-ramp wall segments (inner/outer walls reused the same sibling index range instead of continuing sequentially) -- caught by a full-file duplicate-index audit script before it ever got committed; (b) a genuinely pre-existing, previously-undetected bug from several commits earlier this session -- RightGuideSprite's color override was using the same index as LeftGuideSprite, meaning the right guide's recolor likely wasn't actually applying at all until now. Also verified the runtime wiring with more than a parse check: `godot4 --headless --scene tables/the_glitch/the_glitch.tscn --quit-after 10` actually executes _ready() for real frames (the standard `--quit` headless check never does, since main_scene is the physics prototype, not this table) -- zero runtime errors, confirming every new node-path lookup resolves.
  88/88 tests passing across all commits. Explicitly NOT done despite the "build everything" instruction, flagged honestly rather than silently: the two orbit loops don't cross each other like the reference image (simplification to avoid stacking a second physics-layer-crossing system on the one already built for the cyber-ramps); the skill-shot lane is a separate new channel, not a literal single continuous lane from the bottom plunger to the top (would have required modifying the existing, already-tested lower ramp/loop system). Per the agreed build strategy, no live playtesting has happened on any of this new content yet -- that's the next, separate, dedicated pass.
- 2026-07-15 (cont. 27): Designer shared 5 new layout reference images (docs/inspiration/layout/) and clarified scope: 4 are the same wireframe-blueprint style already used, 1 is a fully rendered glossy sci-fi concept -- confirmed the rendered one is layout-identical to the blueprints, just different rendering (rendering style is a separate, later "real art pipeline" conversation, explicitly deferred). Confirmed the uniform-scaling approach already in place (fixed reference resolution + letterbox) is correct going forward -- no per-device layout reflow needed, matching how real physical pinball cabinets work. Flagged honestly that no actual `viewport_scaler.gd` responsive system exists yet, just the fixed-canvas-items stretch mode -- noted for future work, not blocking.
  Rebuilt the lower playfield to read as a connected "road" per the blueprint: added visual-only curved rail sprites (solid core + glow, same additive technique as everywhere else) tracing wall -> outlane post -> slingshot -> flipper as one continuous path on both sides, inserted early in the draw order so they render underneath the interactive elements like an actual road. Reshaped the slingshot sprites into a proper 4-point kite silhouette matching the blueprint instead of a plain triangle. Deliberately did not touch any collision geometry for either change -- the right side's inlane/outlane collision cost multiple rounds of live-tested fixes earlier this session and wasn't worth re-risking for a decorative pass. Hit and fixed a real index-numbering mistake mid-edit (inserted rails in the wrong draw-order position first, causing duplicate sibling indices) -- caught and corrected before committing, verified no duplicate/orphaned nodes afterward. 66/66 tests passing.
- 2026-07-15 (cont. 26): Designer asked to solo-brand every production source file (name, repo, Patreon, license line, tagline) plus a footer crediting Claude Code -- confirmed solo dev, no other collaborators. Flagged before doing it: the repo was already public with no LICENSE file at all (a real gap per the standing license-check habit for this project), and the designer's exact stated intent (public for GitHub's free CI/CD, not free to copy) is precisely what "no license = full copyright by default" already means -- added an explicit LICENSE file making that unambiguous instead of implicit, rather than inventing a named license. Applied headers/footers to 17 .gd + 1 .gdshader production files (core/, tables/, autoload/), using `#` for GDScript and `//` for shaders since GDScript doesn't support C-style line comments -- the designer's original template as given would have been a syntax error. Deliberately skipped test/ and spike_10b/ (throwaway) to keep the header meaningful.
  Continued the visual pass: gave both flippers the same neon-outline treatment as the slingshots (bright fill + glow + crisp edge), and fixed a real bug found in the process -- both flippers were using the exact same hardcoded pink color from the original §10a placeholder, not the cyan-left/magenta-right split every other themed element already follows. Then, per designer pointing back at the blueprint reference, reshaped the flipper *visual* polygon from a flat rectangle to a proper tapered paddle shape (wide at the pivot, narrow rounded tip) -- left the CollisionShape2D completely untouched since that's the tuned, playtested physics hitbox and this is purely cosmetic. 66/66 tests passing across all commits.
- 2026-07-15 (cont. 25): Started the real visual pass, agreed after designer flagged the table looking far worse than the 1992 reference inspiration — fair, since nothing before this point had a background or unified palette, it was all flat §10a placeholder shapes. Two increments: (1) added the table's first background at all -- reused (not reinvented) the animated grid shader from the §10b performance spike, which already passed its real-hardware 60fps test, copied into tables/the_glitch/fx/ rather than referenced from spike_10b/ directly since that folder is explicitly throwaway per the GDD. Tuned to the established cyan palette. (2) Recolored the flat mid-gray walls/guides to a dark blue-black matching the new background, via per-instance property overrides in the_glitch.tscn rather than editing the shared core/physics/physics_prototype.tscn -- verified with a temporary headless probe that this actually overrides the existing sprites rather than silently duplicating nodes. 66/66 tests passing across both commits.
- 2026-07-15 (cont. 24): Designer looked at RightGuide directly in the editor and called out the previous fix correctly: it was a 28px stub next to LeftGuide's full 169.7px length, an obviously asymmetric band-aid, and the save-post added on top of it in the last commit was a second band-aid rather than a real fix. Replaced both with a proper guide at a shallow 5.7° angle instead of the original 45°: (290,600) to (390,610), length 100.5. The original 45° angle is fundamentally incompatible with the ramp corridor's clearance requirement (a 45° line spanning that x-range necessarily rises above the y=460 mouth, as proven two commits ago) -- a shallow angle sidesteps that entirely, staying at y=600-610 (150+ units clear of the mouth) across its whole span, while actually functioning as a real funnel again instead of a token 28px nub. This made RightFlipperSavePost redundant (the guide now covers the same ground properly) -- removed. 66/66 tests passing.
- 2026-07-15 (cont. 23): Cowork stopped remote-testing the loop shot itself (diminishing returns polling for a deliberately-aimed flipper shot via position/velocity snapshots) and recommended a human playtest pass instead — agreed, and agreed to pivot to the visual pass next. Designer independently observed the same underlying symptom live: balls near the right flipper bounce off and drain, never reaching it. Traced the actual cause rather than the symptom: RightFlipper's collision arm only ever covers x=220-310 (its sprite/collision offset is entirely in -x from the pivot, by construction), so any ball landing in the x=330-395 ramp corridor — which has to stay open for the loop shot, can't be walled without re-blocking the ramp entrance fixed two commits ago — falls through completely untouched, a 100% guaranteed drain for that whole corridor width, not just bad luck. Added a small static post (RightFlipperSavePost) at (350,605), below the ramp mouth with a wide margin and outside the flipper's actual reach, so a falling ball has something to deflect off instead of an untouched drop. Flagged honestly as a reasonable evidence-based mitigation, not a guaranteed fix — real confirmation needs the human playtest pass already agreed on, not more geometry math. 66/66 tests passing.
- 2026-07-15 (cont. 22): Designer caught a live regression mid-Cowork-session: the RightGuide shortening fixed the loop-blocking bug but left the whole area under the ramp mouth (x~310-405, y=460-620) with no geometry at all except the small outlane post, so the ball just fell straight through to the drain — exactly the tradeoff flagged (but not yet fixed) in that commit. Added a vertical wall (OuterWallExtension) flush with the ramp's OuterWall, closing the gap into a proper outlane channel. Didn't restart the editor for this one since Cowork was mid-session — didn't want to interrupt a live test again after already doing so once by accident.
  Cowork's follow-up retest reported the ball still getting deflected before reaching the loop, attributed to RightGuide — but the actual math said that shouldn't be possible with the already-shortened guide (confirmed nowhere near the corridor). Restarted the editor (both fixes now loaded) and asked Cowork to verify the live node geometry directly via MCP rather than inferring the cause from bounce behavior alone.
  Cowork's next pass used the debug teleport hook and got clean, conclusive results: (1) RightGuide geometry confirmed exactly as expected, not the actual cause of anything. (2) Both saucers work correctly end-to-end, confirmed via capture/eject log lines for each, and the "DUAL CAPTURE" objective fired correctly as a bonus proof of the two-target hit_targets wiring. (3) A real, reproducible bug in the loop shot: teleporting the ball straight into the empty channel (x=362, well inside the clear corridor) still ejected it sideways into unrelated geometry near the drain, with no loop trigger, twice identically -- ruling out reachability as the issue. Diagnosed with Cowork's own hypothesis confirmed as the likely cause: RigidBody2D's CCD_MODE_CAST_SHAPE sweeps from the body's pre-teleport position to its new one each physics step, so a direct `_integrate_forces` transform write can still produce a solver collision against whatever geometry happens to lie along that stale, physically meaningless sweep line. Fixed by disabling CCD for 2 physics frames after any teleport (both the debug hook and the pre-existing respawn path, which shares the exact same risk and just hadn't surfaced it yet), then safely re-enabling it. Added a regression test locking in the disable/re-enable timing. Still open per Cowork's own flag: worth confirming a real (non-teleported) high-speed loop shot behaves correctly too, in case there's a second issue independent of teleporting. 66/66 tests passing across this entry's commits.
- 2026-07-14 (cont. 21): Cowork reported ECONNREFUSED trying to reach the MCP server. Diagnosed: found the correct godot4 editor was fine, but a *second*, wrong Godot instance was also running in parallel (`/snap/godot-4/29/godot-4` — the Mono/.NET build flagged as the wrong install earlier this session) plus a leftover `godot-mcp-server` node process squatting the port, orphaned from an earlier editor restart. Killed everything, verified the port was genuinely empty (not just "Godot says connected"), relaunched a single clean godot4 instance. Confirmed the rogue Mono editor had also silently written a stray `[dotnet]` section back into project.godot — same bug class already documented and fixed once earlier this session — removed it again.
  Cowork's actual playtest report (once reconnected) had three real findings:
  1. **Loop mouth widened correctly, but a second, different blocker exists.** RightGuide (diagonal wall, (290,600) to (410,480)) intercepts the ball before it can rise above the y=460 ramp mouth. Verified with real numbers before touching anything: across the full corridor (x=320-405), the guide sits at y=485-570, solidly below the mouth threshold — not a marginal case. Root cause: the earlier fix only checked clearance at the guide's single highest endpoint (x=410), not its extent across the whole corridor width. Fixed by shortening RightGuide to (290,600)-(310,580), clearing the corridor. Traded off and flagged honestly: reduced outlane funnel coverage between x=310-410 — worth watching next playtest, not a free fix.
  2. **Saucers unreachable via blind flipper-cascade play** (y=-480/-560, too far up for the RNG technique that worked at y=-260/-300). Cowork explicitly asked for a debug hook rather than more blind physics chasing.
  3. **Crowding in the upper zone confirmed** by an actual screenshot — six-plus distinct elements stacked in the top ~40% of a 420-wide table. Flagged back to the designer as a genuine design/pacing call, not something to unilaterally cut.
  Built the debug teleport hook Cowork asked for: `ball.gd` gets `request_teleport(position, velocity)`, built on the same `_integrate_forces` pattern already proven safe for the respawn logic (added a regression test guarding against the exact freeze bug that pattern was built to avoid). `the_glitch.gd` wires number keys 1/2/3 to teleport to Saucer/Saucer2/loop mouth, gated behind `OS.is_debug_build()` so it compiles out of release exports. 65/65 tests passing.
- 2026-07-14 (cont. 20): Designer said push for more content, then hand off to Cowork for a live testrun after this next batch. Added a second saucer and second spinner (blueprint shows both paired on either side) — pure reuse of the already-tested saucer.gd/spinner.gd, zero new logic, low risk to stack before a playtest. Then tied both saucers together with a "saucer_double" hit_targets objective (capture both, any order, for a bonus) rather than leaving them as two disconnected standalone triggers — reused the exact same pattern as the standup bank. Stopping here to let Cowork actually play it: a lot of geometry (loop cap, saucer placements, two new zones) has only been verified by collision math this session, not a real ball. 61/61 tests passing across all commits.
- 2026-07-14 (cont. 19): Designer's follow-up on the blueprints: build a saucer/kickout hole, skip captive balls for this table, and the ramp/loop idea is a go. Built `core/physics/saucer.gd` — a genuinely new interaction shape versus everything else in core/physics/ (targets/spinner/bumper all react to a pass-through; a saucer captures the ball, freezes it in place, holds briefly, then ejects with an impulse). GUT-covered including the capture/freeze/snap behavior and the post-hold eject velocity (had to loosen the assertion tolerance from 2.0 to 5.0 — small linear-damping drift accumulates over the real-time `wait_seconds` used to let the hold timer elapse, unlike the instant-impulse slingshot/bumper tests). Placed in the new upper zone at (200,-480), clear of the cache bumper cluster below.
  Extended the shared ramp (physics_prototype.tscn) up into the new headroom for a "high-speed loop" shot — deliberately left the bottom entrance geometry, tuned across two earlier live-playtest rounds, completely untouched, only moved the tops from y=-300/-200 to a matched y=-600 and capped the channel with a RampCap wall. Caught and fixed a real gap on the first pass: the cap was positioned 7 units above the wall tops instead of touching them, which would have let the ball sneak through — checked the actual numbers before considering it done. Added a HighSpeedLoop scoring sensor at the crest as a direct-trigger score, not a full separate physical return lane back into the playfield — that's a bigger, genuinely different piece of geometry (an orbit needs a second lane, not just a cap) that deserves a live-tested pass of its own rather than another blind stack on top of everything else built this session. 61/61 tests passing.
- 2026-07-14 (cont. 18): Designer approved continuing, with explicit guidance: keep building until it feels crowded, then stop placing objects and extend the table upward rather than cramming — room for more content later rather than now. Added a rollover gate (direct-trigger scoring lane, same pattern as Clock Lane) in the last open flat gap (x=50-110/y=0-150), which closed out the middle zone's remaining floorspace. Then extended the table 300px upward using the exact same method as the earlier crowding fix (cont. 7): boundary walls/camera re-centered around a fixed bottom edge (y=700), TopWall moved from y=-350 to y=-650, TouchZones shifted +300 (screen-space CanvasLayer content doesn't track the camera, has to be repositioned by hand). Notably this time the Feedback UI needed zero changes — the fractional/bottom-anchored fix from cont. 15 absorbed the resize automatically, unlike the raw-offset touch zones. This lives in the shared core/physics/physics_prototype.tscn base, so all future tables inherit the extra headroom. New top region (y=-650 to -350) is deliberately left empty, reserved for the blueprint's upper-zone content (saucers, captive balls, high-speed loops) rather than pre-built without a live playtest to inform it. 57/57 tests passing across all three commits this entry.
- 2026-07-14 (cont. 17): Designer shared classic EM/solid-state pinball layout blueprints as reference and gave full autonomy to keep implementing without stopping for per-task confirmation. Kept scope inside CLAUDE.md's existing guardrails (Phase 1 only, no parallel agents editing the same scene file — too risky for Godot's fragile node-index .tscn format, sequential work instead). Four increments, each independently tested/committed/CI-verified:
  1. Redesigned both slingshot visuals: flat solid-color triangles → dark semi-transparent rubber-pad fill + crisp neon edge + soft additive-blend glow halo (cyan left / magenta right, matching the flippers). First real visual-design work on the table — everything before this was flat placeholder geometry per the locked physics-first priority order.
  2. Found and fixed the actual bug behind the designer's "slingshots are weirdly placed" complaint: RightSlingshot was at x=240 (near table-center), never actually mirrored against LeftSlingshot's x=135 relative to its own guide's endpoint (x=290) the way the left one was. Repositioned both to funnel correctly against their guide, and added small static outlane divider posts (dead rubber post) at the inlane/outlane split on each side, referencing the blueprint's lower-playfield layout.
  3. Added the first middle-zone gameplay element: a 3-bumper pop cluster ("Compile Cluster"), reusing bumper.gd + wire_hit_group as-is, positioned and verified clear of the Firewall Breach targets above and the Glitch Core below by actual collision radii. Wired as a repeatable "compile_overclock" charge objective (10 hits), matching the Sprite Defrag/VRAM Throughput pattern.
  4. Built a new reusable primitive, `core/physics/spinner.gd` (free-swinging vane, emits target_id on pass-through per the standup_target.gd convention, cosmetic spin via Tween rather than simulated hinge), with GUT coverage (test_spinner.gd) — including a real bug caught by the test suite itself: an untyped `_spinning` local var failed GDScript's static-typing inference and silently made GUT skip loading the whole test file (fixed by explicit `: bool` annotation). Placed one spinner in the open right-side lane on The Glitch.
  57/57 tests passing across all four commits; CI green on each push.
- 2026-07-14 (cont. 16): Second MCP-driven playtest, same velocity-informed methodology. Slingshot fix confirmed working (no more redirect-across-the-table incidents), but the ramp still didn't land — this time with hard evidence rather than vague difficulty: shots consistently topped out at x=320-334, a repeatable ~20-unit shortfall from the inner wall at x=345, across multiple different launch paths (not scattered variance). Real geometric gap, not a timing skill issue. Cowork's proposed fix (move the inner wall left ~20-25 units) was right in principle, but I checked the exact numbers before applying it and found doing that alone would put the wall directly on top of CacheBumperB (x=320, radius 18) — Cowork's assessment that the bumper wasn't interfering was about ball trajectory, not the wall's own static collision geometry, which is a different question. Moved both: InnerWall x=350->325 (widens the channel from 40 to 65px, landing right where the ball's real arc apexes) and CacheBumperB x=320->285,y=-160->-170 (clears the new wall position, still fits the existing bumper cluster shape). 54/54 tests passing.
- 2026-07-14 (cont. 15): Got the Godot MCP plugin (github.com/tomyud1/godot-mcp, MIT) installed and connected — Cowork (Claude Desktop, via the npm godot-mcp-server the designer set up) can now inspect and live-play the running editor over WebSocket on :6505 rather than only reading files statically. First real MCP-driven playtest report came back with four genuinely actionable findings, all addressed:
  1. **Critical: uncapped scoring exploit in bumper.gd.** No cooldown at all — a ball rattling inside the circular Area2D could re-trigger body_entered many times per second, each firing full points (observed live: score jumped 15850->60175 in ~2 seconds of normal play). Added the same cooldown pattern slingshot.gd already had. Added test_bumper.gd (didn't exist before) with a regression test for this specific failure mode.
  2. **Ramp still unreachable — real cause identified via velocity-informed live testing, not guessing.** Cowork read live linear_velocity, timed flipper taps precisely, and confirmed genuine flipper-driven shots toward the ramp's x-range were getting deflected by RightSlingshot (positioned almost exactly in the flight path). Moved the slingshot out of the corridor. Did NOT also widen the ramp mouth as suggested, since that would collide with CacheBumperB (checked the geometry first) — fixing the clearly-identified primary blocker before making further speculative changes.
  3. **UI anchoring gap, confirmed real.** ScoreLabel/HighScoreLabel/message Label/DebugTerminal were all raw offset_* with no anchors — worked only because of the fixed reference resolution + letterboxing, not because they adapt. DebugTerminal was the risky one (910px from a 1050px-tall canvas, unanchored — would clip if that height ever changed). Anchored it to the bottom edge properly (anchor_top/bottom=1, negative offsets) and centered the feedback message label with fractional anchors instead of an absolute offset.
  4. **Architecture review + real duplication caught.** Confirmed the objective system is genuinely theme-agnostic and would support a second table without touching core/ — but flagged two things: (a) objective_manager.gd's comments overstated what's built (claimed objectives.json loading exists; it doesn't, config is a hardcoded GDScript array in the_glitch.gd) — fixed the comments to accurately describe current state and explicitly defer building the JSON loader until a second table needs it. (b) the_glitch.gd hand-wired near-identical "add_score + register_target_hit + log" triples for every target group — extracted `wire_hit_group()` onto PinballTable base (generic, reusable across tables) and rewired the_glitch.gd to use it, removing four blocks of duplication.
  Added test_pinball_table.gd for the new helper; hit a real GDScript gotcha in the process (lambdas capture outer local variables by value, not reference — a plain `var` reassigned inside a closure doesn't propagate out; fixed by mutating an Array instead). 54/54 tests passing.
- 2026-07-14 (cont. 14): Still couldn't reach the ramp after the guide-overlap fix. Designer tried adjusting the walls directly in the editor and suspected something went wrong — correct: found the PhysicsPrototype instance in the_glitch.tscn had picked up an accidental position offset (0, 20), desyncing the ramp/walls/flippers from everything else on the table (which stayed at their original coordinates), and the ramp's InnerWallSprite (visual) had drifted 32px from its actual collision shape. Fixed both. Also realized a likely deeper issue while re-examining the geometry: the ramp entrance sits at x=350-400, which is outside the right flipper's reach entirely (its arm only swings toward table center, i.e. leftward from its x=310 pivot) — reaching it requires an indirect free-flight shot, not a direct hit, which is a much harder/lower-margin shot. Flagged honestly rather than guessing at more geometry changes blind; next step is designer confirming whether it's reachable at all now that the real bugs are fixed, before further tuning.
- 2026-07-14 (cont. 13): Designer couldn't reach the ramp at all ("obstacles in the way"), and separately assumed the ramp launches a new ball — clarified that's a different mechanic (the plunger/launch key). The real issue: rebuilding the ramp didn't account for the pre-existing RightGuide (a 45°-angled inlane wall funneling toward the right flipper), which crosses diagonally right through the ramp's entrance zone (computed its endpoints: roughly (290,600) to (410,480), directly overlapping the ramp mouth at x350-400,y480-560) — the entrance was physically blocked. Raised the ramp's bottom edge to y=460, clear above the guide's highest point, rather than guessing at a fix blind; verified the geometry doesn't touch anything else it shouldn't. Shot feasibility (can a real flipper shot actually reach that height along the guide's slope) still needs the designer's hands-on confirmation — flagged as unverified rather than assumed.
- 2026-07-14 (cont. 12): Continued into backlog with VRAM Pipeline. Discovered the §10a ramp geometry (OuterWall/InnerWall) was completely gone — an empty container node, no collision shapes at all — from when the designer removed what looked like a duplicate wall earlier in the session. Confirmed with designer before touching it: rebuilt the ramp, and since the table's now taller (from the crowding fix), extended it the full new height (y=-300 to 560, up from the original y=60-560) rather than just restoring the old short version, so it actually uses the extra space. Added VRAM Pipeline as a rollover sensor at the ramp's exit rather than building a whole new orbit track (would've reintroduced crowding risk right after fixing it) — reuses standup_target.gd + accumulate_hits, same charge/threshold pattern as Sprite Defrag ("VRAM Throughput"). 46/46 tests still passing (no new tests needed, fully reused logic).
- 2026-07-14 (cont. 11): Designer hit a real bug immediately: hit the Firewall Breach targets right-to-left, one dropped (dark) but nothing reset, unclear whether the mode ever completed. Root cause was a design mismatch, not a code typo: drop_target.gd always physically drops on any hit regardless of order, but the objective only resets visuals on a wrong-order hit *after* progress already existed — the very first wrong hit (order violated immediately) doesn't emit sequence_reset at all, so the table never got a signal to pop the target back up. Real fix: order-sensitivity doesn't make sense for targets that stay down once hit — that pattern only works for targets that flash-and-recover (the Shader Nodes). Changed Firewall Breach to require_order=false (hit all 4, any order — matches how real drop-target banks usually work) and removed the now-dead sequence_reset handling for it. 46/46 tests still passing.
- 2026-07-14 (cont. 10): Continuing into backlog content. Found and fixed a real bug while reviewing the existing code: shader_rebuild's underlying HitTargetsObjective never reset after first completion (only Sprite Defrag got a reset() call), so hitting the three shader targets again did nothing for the rest of a play session — is_complete() silently blocked all further notify_target_hit calls. Added reset() to HitTargetsObjective (mirroring AccumulateHitsObjective's) and now call it after the Core is stabilized, so the full repair loop actually repeats within one session, not just across editor restarts. Built Firewall Breach drop targets next: `core/physics/drop_target.gd` — a target that visually drops/fades on hit and stops registering further hits until reset, distinct from standup_target.gd's flash-and-recover (fits "wall you break through" better than a target that instantly springs back). Placed as a literal horizontal wall of 4 targets at the table's old midline (y=0), reusing hit_targets (ordered sequence) rather than inventing a new objective type. Wrong-order hits now also pop the drop targets back up (they don't reset on their own), matching real drop-target-bank behavior. 46/46 tests passing.
- 2026-07-14 (cont. 9): Designer said keep pushing. Chose the replay/input-log recorder over more backlog table content — GDD/CLAUDE.md explicitly call it foundational infrastructure to build now, not later ("much harder to retrofit later"), and it's independent of table space, so no crowding risk right after just fixing crowding. `core/replay/input_log.gd`: records timestamped flipper/nudge/launch events during play (Phase 4 will re-simulate this server-side to verify a submitted score, per GDD §6/§7). Deliberately v1-scoped: captures and holds the log in memory only — no persistence, no upload, no Phase 4 schema, since none of that exists yet to build against. Wired into `PinballTable` base itself (not the_glitch specifically) so every future table gets recording for free, starting automatically in _ready(). 40/40 tests passing.
- 2026-07-14 (cont. 8): Last MVP-tier item from the phased plan: Debug Terminal display. Started `core/ui/` for real (was empty) with `debug_terminal.gd`/`.tscn` — a small reusable scrolling event-log widget, deliberately just the one piece needed now rather than building out the full responsive HUD framework GDD §11 describes ahead of need. Wired into the_glitch.gd: every shader-target hit, cache-bumper hit, and major mode event (Shader Rebuild, Sprite Defrag, Core Stabilized, Clock Sync, Sequence Reset) now logs a line. All three MVP-tier interactions plus this display are in — that closes out everything in that tier from the_glitch_table_layout.md's phased plan. Remaining is explicitly backlog (RAM lanes, VRAM orbit, Firewall Breach, mini-playfield) — not scheduled. 34/34 tests passing.
- 2026-07-14 (cont. 7): Designer flagged real crowding — with all three MVP-tier interactions in, the 420x700 playfield had shader targets, cache bumpers, the Glitch Core, ramp, and bumper all bunched in a narrow band. Enlarged the table by extending the boundary *upward only* (new top at y=-350, viewport height 700->1050) rather than resizing everything, so the already-tuned flipper/slingshot/bumper/ramp/guide geometry near the bottom stays pixel-identical and doesn't need re-validation — only the newly available top region needed populating. Redistributed the Shader Node targets, Sprite Cache bumpers, and Clock lane into that new space with real gaps between them. Caught a real gotcha along the way: TouchZones (touch controls) live in a CanvasLayer, which is screen-space and untouched by the Camera2D's move — so moving the camera to re-center on the taller table silently desynced the touch buttons from the flippers on screen until corrected (+350 offset, matching the camera's shift). The Feedback message label had the same issue; the corner Score/High Score labels didn't (they're screen-anchored, not tied to any world element). 31/31 tests still passing (all scene geometry, no logic changed).
- 2026-07-14 (cont. 6): Designer confirmed Sprite Defrag works live. Added the CPU Clock Cycle lane + Clock Sync — simpler than the other two modes since it's a single direct trigger, not a multi-step objective, so it reuses standup_target.gd as-is and skips the objective system entirely (not every scoring element needs to be a tracked objective). All three MVP-tier table elements from the phased plan are now in: Shader Rebuild (ordered sequence), Sprite Defrag (charge/threshold), Clock Sync (direct trigger) — three genuinely different objective/interaction shapes proven out on one table. Remaining from that tier: Debug Terminal display. 31/31 tests still passing (no new tests needed — reused already-tested logic).
- 2026-07-14 (cont. 5): Designer asked whether layout/UI had been tackled yet — clarified it hasn't (pure function so far, per the deliberate CLAUDE.md priority order), designer chose to keep following the plan rather than pause for it now. Built the next MVP-tier piece: Sprite Cache Bumpers + Sprite Defrag mode. Added `core/objectives/objective_types/accumulate_hits.gd` — a second generic objective shape (build up hits on any of a target set until a threshold, order-independent, repeatable via reset()) distinct from hit_targets.gd's one-shot ordered sequence. Extended bumper.gd with an optional target_id/hit signal (backward compatible — existing bumpers unaffected) so it can feed the objective system like standup_target.gd already does. Three new bumpers on the table feed "sprite_defrag" (6 hits across any of them); completing it awards a bonus and resets for the next charge, unlike the one-shot shader sequence. Hit a real test-isolation bug: GameState persists high scores to an actual file, so a stale entry from an earlier test run leaked into a later run and broke two tests — fixed by clearing the test table's entry before/after each test rather than assuming a blank slate. 31/31 tests passing.
- 2026-07-14 (cont. 4): Designer confirmed the full Core loop works live. Pushed all pending commits to GitHub, confirmed CI green (GitHub Actions, GUT suite, ~20s), made the repo public per designer's request (ran a quick history scan for secrets first — clean). Then closed out the last piece GDD's vertical-slice definition explicitly names but wasn't built yet: local high score. Added `autoload/GameState.gd` (session score + local high-score persistence to user://high_scores.json) — deliberately a single autoload rather than GDD §11's fuller player_progress.gd/ProgressionService split, since that split exists for a Phase 4+ server-sync seam that doesn't matter yet; noted as a scoped-down deviation to revisit when that's actually true. Wired scoring into the_glitch.gd for every interactive element (shader targets, Core charged/uncharged hits, slingshots, bumper — added a `kicked` signal to bumper.gd for this, same pattern as slingshot.gd), plus live score/high-score labels. No real "game over" event exists yet (ball just respawns indefinitely), so the high score updates live rather than at some end-of-ball event that doesn't exist. Added 6 GUT tests for GameState. 26/26 total passing.
- 2026-07-14 (cont. 3): Confirmed the sequence-reset feedback fix works live (designer saw both "SHADER REBUILD COMPLETE" and "SEQUENCE RESET" correctly). Built the next piece of the vertical slice per the phased plan: The Glitch Core. `core/physics/rotating_toy.gd` is a generic reusable toy (spins continuously, can be toggled "charged" externally, reports which state it was hit in) — deliberately theme-agnostic, same pattern as standup_target.gd. Wired into the_glitch.gd: completing Shader Rebuild charges the Core (visual + state change), hitting it while charged shows "CORE STABILIZED" and resets it, closing the repair loop (targets → mode → Core → repeat). Added 5 GUT tests for the toy logic (17/17 total passing). Also fixed the local dev environment: this machine ran critically low on memory (down to ~900MB free / swap nearly exhausted) mid-session from unrelated background apps, which was blocking the Godot editor from launching — cleared once the designer closed other applications; killed two of our own leftover helper processes (cloudflared tunnel, python http.server) that were no longer needed after mobile testing.
- 2026-07-14 (cont. 2): Designer reported hitting the Shader Node targets (saw the yellow hit-flash) but never saw the completion text. Chased this down a long false trail first — repeated headless CLI runs of the actual game scene were hanging/timing out, which looked like a real bug in the new table-base/objectives code. Root cause turned out to be system memory pressure (this dev machine was down to ~600MB-1GB free RAM with swap nearly exhausted from several heavy browser/Electron processes), not the code — confirmed by testing the previously-proven-working physics_prototype.tscn scene, which hung identically. GUT test runs (which don't need to spin up a full running scene) were unaffected throughout and kept passing, which is what actually proved the objective wiring itself was correct. The real, much simpler explanation: require_order=true silently resets progress on a wrong-order hit with zero feedback, which reads exactly like "nothing happened" — almost certainly what the designer hit. Fixed properly rather than just diagnosed: added a sequence_reset signal (only fires if real progress existed, not on an immediate wrong-first-hit) threaded from HitTargetsObjective -> ObjectiveManager -> PinballTable, with the_glitch.gd now flashing the targets red and showing a "SEQUENCE RESET" message so a wrong-order hit is visible instead of silent. Added 2 more GUT tests for the new signal (14/14 total passing). Also added docs/.gdignore so Godot's project scanner never tries to import the reference mood-board images under docs/ as game assets — unrelated to the actual hang, but correct regardless.
- 2026-07-14 (cont.): Started the vertical slice. Designer gave full sequencing autonomy and asked to build "bit by bit," well-designed over fast, so started with the structural foundation rather than jumping to visuals: `core/table_base/pinball_table.gd`+`.tscn` (base class every table extends — currently just routes target hits to the objective system and exposes a ball_lost hook), and a minimal `core/objectives/` system (`objective_manager.gd` reads a config array — normally objectives.json per GDD §6 — and instantiates matching objective types; `hit_targets.gd` is the first reusable type: hit a configured set of targets, optionally in a specific order, with wrong-order hits resetting progress). Built the first real table content on top: `tables/the_glitch/the_glitch.tscn` — the Shader Node Targets (3 standup targets, triangle formation, left side per the layout doc) wired to a "Shader Rebuild" objective. The table scene reuses the §10a physics prototype via scene instancing (same pattern already proven in the §10b spike scene) rather than Godot's literal "inherited scene" feature, to avoid the fragility of hand-authoring that format — same reuse goal, more reliable to author by hand. Added GUT test coverage for the objective logic (test_hit_targets_objective.gd, test_objective_manager.gd) — 10/10 tests passing, including the wrong-order-reset behavior which is exactly the kind of thing worth pinning down before building more objective types on top of it. Not yet wired: any visual feedback beyond a plain placeholder label, the Glitch Core toy, or scoring.
- 2026-07-13 (cont. 3): Started §10b (performance/effects spike). Confirmed the project's locked renderer (GL Compatibility, project.godot) does not support Godot's built-in Glow/bloom post-processing — that requires the Forward+/Mobile renderer — so "neon glow" is faked via additive-blended sprites instead, which is also cheaper on low-end mobile. Built `spike_10b/performance_spike.tscn` (deliberately kept outside `core/`/`tables/` since it's throwaway per GDD, not production Glitch content): instances the §10a physics prototype unmodified, layered with the risky elements GDD §10b calls out together — 4x PointLight2D dynamic lights, 3x CPUParticles2D emitters, a screen-space RGB-split glitch shader (SCREEN_TEXTURE read — required an API fix for this Godot version: `hint_screen_texture` uniform syntax, not the old built-in), an animated scrolling-grid background shader, 5 decorative orbiting objects, and an on-screen FPS counter for the actual decision-gate readout. Verified headless (no script/shader errors) but that only proves it loads — real frame-time measurement needs an actual running window, which is next.
- Export templates for HTML5 are NOT installed on this dev machine — needed before the actual mobile-hardware test GDD §10b requires. Flagged to designer, not yet downloaded (own decision point, ~200MB+ download).
- 2026-07-13 (cont. 4): Discovered the Godot install being used (`godot-4` snap) was actually the Mono/.NET build, not the plain GDScript build the project calls for — this project has zero C# code, but a Mono-flavored editor still bundles the Mono/.NET web runtime into HTML5 exports regardless, which would have bloated the real export and added hosting requirements (cross-origin isolation headers) for no benefit. Tried installing the correct plain build via Flatpak first, but it pulled in a very large freedesktop SDK runtime dependency and was too slow to be worth waiting on; switched to downloading the official standalone Godot 4.7 Linux binary directly (~75MB, no runtime dependency), confirmed non-Mono (`4.7.stable.official`, no `.mono` tag), symlinked as `godot4` on PATH. Set up proper automated testing per designer's request: installed GUT (Godot Unit Test) as `addons/gut` (had to bump from v9.3.0 to v9.7.1 — the older version's `Logger` class name collided with a native class Godot 4.7 introduced, a real version-compatibility bug, not a project bug), wrote first regression tests (`test/unit/test_flipper.gd`) covering the rest/active angle mirroring bug fixed earlier this session, wired `scripts/run_tests.sh` for local runs and `.github/workflows/tests.yml` so tests run automatically on every push. Also fixed `project.godot` for real this time (removed the stray `[dotnet]` section, restored `[physics]`) and got `export_presets.cfg` set up for a Web (HTML5) export target, intentionally committed (no secrets) so CI can reproduce the same build.
- Noted for the designer: GUT/GDUnit/GodotTestDriver/MPF-style tools test game *logic*, not rendering performance — they're the right tool for future scoring/objective regression coverage, but can't answer §10b's actual "does this run smoothly on real hardware" question. That still requires an actual device (phone/tablet) or a cloud real-device farm (BrowserStack etc.), not unit tests or browser dev-tools emulation.
- 2026-07-13 (cont. 5): Got a real HTML5 build onto the designer's iPhone. Hit and fixed a chain of real export issues: (1) hand-written `export_presets.cfg` failed with an opaque, empty "configuration errors" message — bisected it down to `vram_texture_compression/for_desktop`/`for_mobile`, which aren't valid options for the Web platform despite appearing in the binary's strings (they belong to a different platform); removing them fixed it. (2) Godot's data dir resolved to a VS Code snap sandbox path (`XDG_DATA_HOME` override) rather than the real home directory, so export templates had to be copied there too, not just `~/.local/share/godot`. (3) Browsers require a secure context (HTTPS or localhost) for the WebGL/threading APIs the export needs — serving plain HTTP over the LAN IP failed on iOS Safari; solved with a Cloudflare quick tunnel (no account needed) for a real HTTPS URL. (4) The touch-zone flipper controls silently didn't work on the real device: `LeftZone`/`RightZone` had been reusing the `wall_top` collision shape (420x20, a 20px sliver at the very top of the screen) as a leftover placeholder — never actually sized for touch input. Replaced with dedicated 130x130 corner zones. (5) Second bug on top of that: `shape_centered=false` didn't shift the actual tap-detection hit-region the way it shifted the visual (hit region stayed centered on the button's `position`), so only one corner of the visible box was tappable. Fixed by switching both the button and its visuals to a centered coordinate scheme, and added a circular visual indicator inside the square tap zone per designer's UX feedback. Confirmed working live on iPhone Safari: game loads, physics runs, both flippers respond reliably to touch.
- 2026-07-14: Tested the actual §10b effects spike on the designer's iPhone (temporarily pointed main_scene at spike_10b/performance_spike.tscn for export, then reverted back to physics_prototype.tscn after). Result: steady 60fps, no lag, fully playable — GDD §10b decision gate passes. Designer confirmed the spike's placeholder lights/particles/orbiting shapes are not to their taste, which is fine and expected — flagged clearly to them that none of this is real Glitch art direction, just deliberately generic worst-case load for the performance test. Real theme/visual identity work for The Glitch hasn't started and is the next real design decision, not something this spike should be read as predicting.
- 2026-07-13 (cont. 2): Flipper power tuning stalled — designer raised torque well past where it made any perceptible difference, then overcorrected into an unstable spin. Sent two parallel research agents (Godot-specific implementations + general flipper-physics/Visual Pinball reference) rather than keep guessing. Both converged on the same finding: a torque+PD-controlled RigidBody2D plateaus because Godot's collision solver derives the ball's impulse from the flipper's actual measured velocity at the exact contact tick, capped by how far torque can accelerate the body within one physics step — more torque past that ceiling doesn't help and destabilizes instead. The fix used by real implementations (Visual Pinball's dedicated flipper solver, and the MIT-licensed reference project `dbisdorf/professor-pinball`) is to drive flipper rotation directly by script over a fixed short duration on a frozen/kinematic body, not via torque at all — a kinematic body's velocity is authoritative to the solver, so it transfers full strength into the ball on contact. Rewrote `flipper.gd` accordingly (eased rotation over `swing_duration_seconds`, `freeze_mode = FREEZE_MODE_KINEMATIC`), which also let us delete the PinJoint2D/center-of-mass workaround entirely — a kinematic body's position is always authoritative, so it can't drift. Added continuous collision detection (`continuous_cd = CCD_MODE_CAST_SHAPE`) to the ball to prevent tunneling at the new higher contact speeds. Verified via a temporary headless probe (removed after use): a well-timed hit produced a ~7x ball speed spike (146 → 997) versus the old torque approach's imperceptible response. Live playtest confirmed: flippers now feel genuinely strong and snappy, designer could hit the ceiling of the current table, which is a good sign for future tables with more vertical space.
- §10a gate status: physics feel (flipper power/response, ball behavior, respawn loop) confirmed good by the designer. Ramp, outlane guides (45° from side walls to each flipper pivot), and pivot bolt visuals also added and playtested. Remaining before calling §10a fully closed: outside playtester feedback (GDD calls for "you and ideally 1-2 outside people" — so far only the designer has played it), and the bumper is still a plain static collider with default restitution (doesn't "kick" the ball harder than a wall would, unlike a real pinball bumper).
- 2026-07-13 (cont.): Second playtest round after fixes. Found and fixed two more real bugs: (1) flippers were spaced too close together (120px apart, 90px arms) so they physically overlapped at rest, and rest/active angles were swapped — flippers drooped further down when pressed instead of swinging up; fixed by widening pivot spacing (110/310) and correcting rest=droop-down/active=swing-up angle signs. (2) The drain/respawn logic wrote `position`/`linear_velocity` directly on the ball's RigidBody2D from `_physics_process`, which fights Godot's physics server transform sync — reproduced via a temporary headless debug probe (removed after fix): after one respawn the ball froze permanently with gravity no longer applying, confirmed in a scripted headless trace. Fixed by moving the teleport into `_integrate_forces()`, the correct hook for direct RigidBody2D state changes — reverified via the same headless trace, ball now falls/drains/respawns correctly on repeat. Also wired the previously-inert `launch_ball` input action to an actual impulse, and moved ball spawn off the bumper's dead-center vertical line. Playtested live: flippers now stay pinned, swing the right direction, ball loops indefinitely without getting stuck. Known gap (acceptable for §10a): no outlane/guide walls yet, so a missed flipper just drains — that's expected table-layout work, not core physics feel, and not yet in scope.
- §10a gate status: physics feel (flipper response, ball behavior, respawn loop) confirmed working end-to-end by the designer. Table layout (guides, ramp, additional bumpers) still open before calling §10a fully "done," per GDD — flagged for next session, not blocking further physics tuning.
- 2026-07-12: Reviewed CLAUDE.md + GDD.md, confirmed scope and understanding. Initialized private GitHub repo (github.com/nobody174/pinball-adventure). Installed Godot 4.7 (GDScript, not Mono) via winget. Built first §10a physics-feel prototype: `core/physics/physics_prototype.tscn` with two flippers, ball, boundary walls, one bumper, placeholder shapes only. Keyboard input (Left/Right arrows = flippers, Space = nudge, Up = launch) and touch zones both wired from the start. Validated headless (imports clean, runs without script errors) — not yet playtested for feel by a human. That's the next step.
- 2026-07-13: First real playtest, on-device. Found and fixed real bugs: (1) boundary walls and bumper had collision shapes but no visuals — added Polygon2D sprites so the table is actually visible; (2) flippers were free-floating RigidBody2D with no pivot constraint, so applying torque about their off-center collision shape caused them to translate away from their pivot instead of just rotating — fixed with a PinJoint2D anchoring each flipper to a fixed StaticBody2D at its pivot, plus forcing center_of_mass back to the pivot in `flipper.gd`. Also added: PD-style damping term to the flipper torque controller (pure P-control could ring at high torque — flagged in `docs/inspiration/physics_notes.md`), and a basic nudge impulse on the ball (cooldown-limited, no tilt lockout yet). Added `docs/inspiration/` (pinball_fantasies_analysis.md, physics_notes.md, mission_ideas.md) — principle-level design notes, not derived from original game code/assets, linked from GDD.md. Not yet re-playtested after these fixes — next step.

## How to Playtest (cabin / any machine)
1. `git pull` in the project folder.
2. Open Godot 4.7, "Import" this folder (project.godot), or just double-click project.godot if Godot is already associated.
3. Press Play (F5) — it should launch straight into the physics prototype scene.
4. Controls: Left/Right arrow keys = flippers, Space = nudge, Up = launch ball (not yet wired to a plunger, ball just starts near the top).
5. What to judge: does the flipper response feel snappy or sluggish? Does the ball ever tunnel through a flipper at speed? Does it feel "right" compared to real/other pinball games? Nothing else matters yet — no art, no objectives, no theme.

## Decisions/Deviations from GDD
(none yet — log anything here that extends or diverges from the locked GDD, per CLAUDE.md "Source of Truth")
