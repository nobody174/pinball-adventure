# Progress Log

## Current Status
- Phase: 1 (MVP — "The Glitch")
- Active milestone: §10b Performance/effects spike
- Gate status: §10a physics feel confirmed good by designer (flipper power/response, bumper kick, ramp, guides). §10b scene built, not yet tested on target mobile hardware.

## Session Log
- 2026-07-13 (cont. 3): Started §10b (performance/effects spike). Confirmed the project's locked renderer (GL Compatibility, project.godot) does not support Godot's built-in Glow/bloom post-processing — that requires the Forward+/Mobile renderer — so "neon glow" is faked via additive-blended sprites instead, which is also cheaper on low-end mobile. Built `spike_10b/performance_spike.tscn` (deliberately kept outside `core/`/`tables/` since it's throwaway per GDD, not production Glitch content): instances the §10a physics prototype unmodified, layered with the risky elements GDD §10b calls out together — 4x PointLight2D dynamic lights, 3x CPUParticles2D emitters, a screen-space RGB-split glitch shader (SCREEN_TEXTURE read — required an API fix for this Godot version: `hint_screen_texture` uniform syntax, not the old built-in), an animated scrolling-grid background shader, 5 decorative orbiting objects, and an on-screen FPS counter for the actual decision-gate readout. Verified headless (no script/shader errors) but that only proves it loads — real frame-time measurement needs an actual running window, which is next.
- Export templates for HTML5 are NOT installed on this dev machine — needed before the actual mobile-hardware test GDD §10b requires. Flagged to designer, not yet downloaded (own decision point, ~200MB+ download).
- 2026-07-13 (cont. 4): Discovered the Godot install being used (`godot-4` snap) was actually the Mono/.NET build, not the plain GDScript build the project calls for — this project has zero C# code, but a Mono-flavored editor still bundles the Mono/.NET web runtime into HTML5 exports regardless, which would have bloated the real export and added hosting requirements (cross-origin isolation headers) for no benefit. Tried installing the correct plain build via Flatpak first, but it pulled in a very large freedesktop SDK runtime dependency and was too slow to be worth waiting on; switched to downloading the official standalone Godot 4.7 Linux binary directly (~75MB, no runtime dependency), confirmed non-Mono (`4.7.stable.official`, no `.mono` tag), symlinked as `godot4` on PATH. Set up proper automated testing per designer's request: installed GUT (Godot Unit Test) as `addons/gut` (had to bump from v9.3.0 to v9.7.1 — the older version's `Logger` class name collided with a native class Godot 4.7 introduced, a real version-compatibility bug, not a project bug), wrote first regression tests (`test/unit/test_flipper.gd`) covering the rest/active angle mirroring bug fixed earlier this session, wired `scripts/run_tests.sh` for local runs and `.github/workflows/tests.yml` so tests run automatically on every push. Also fixed `project.godot` for real this time (removed the stray `[dotnet]` section, restored `[physics]`) and got `export_presets.cfg` set up for a Web (HTML5) export target, intentionally committed (no secrets) so CI can reproduce the same build.
- Noted for the designer: GUT/GDUnit/GodotTestDriver/MPF-style tools test game *logic*, not rendering performance — they're the right tool for future scoring/objective regression coverage, but can't answer §10b's actual "does this run smoothly on real hardware" question. That still requires an actual device (phone/tablet) or a cloud real-device farm (BrowserStack etc.), not unit tests or browser dev-tools emulation.
- 2026-07-13 (cont. 2): Flipper power tuning stalled — designer raised torque well past where it made any perceptible difference, then overcorrected into an unstable spin. Sent two parallel research agents (Godot-specific implementations + general flipper-physics/Visual Pinball reference) rather than keep guessing. Both converged on the same finding: a torque+PD-controlled RigidBody2D plateaus because Godot's collision solver derives the ball's impulse from the flipper's actual measured velocity at the exact contact tick, capped by how far torque can accelerate the body within one physics step — more torque past that ceiling doesn't help and destabilizes instead. The fix used by real implementations (Visual Pinball's dedicated flipper solver, and the MIT-licensed reference project `dbisdorf/professor-pinball`) is to drive flipper rotation directly by script over a fixed short duration on a frozen/kinematic body, not via torque at all — a kinematic body's velocity is authoritative to the solver, so it transfers full strength into the ball on contact. Rewrote `flipper.gd` accordingly (eased rotation over `swing_duration_seconds`, `freeze_mode = FREEZE_MODE_KINEMATIC`), which also let us delete the PinJoint2D/center-of-mass workaround entirely — a kinematic body's position is always authoritative, so it can't drift. Added continuous collision detection (`continuous_cd = CCD_MODE_CAST_SHAPE`) to the ball to prevent tunneling at the new higher contact speeds. Verified via a temporary headless probe (removed after use): a well-timed hit produced a ~7x ball speed spike (146 → 997) versus the old torque approach's imperceptible response. Live playtest confirmed: flippers now feel genuinely strong and snappy, designer could hit the ceiling of the current table, which is a good sign for future tables with more vertical space.
- §10a gate status: physics feel (flipper power/response, ball behavior, respawn loop) confirmed good by the designer. Ramp, outlane guides (45° from side walls to each flipper pivot), and pivot bolt visuals also added and playtested. Remaining before calling §10a fully closed: outside playtester feedback (GDD calls for "you and ideally 1-2 outside people" — so far only the designer has played it), and the bumper is still a plain static collider with default restitution (doesn't "kick" the ball harder than a wall would, unlike a real pinball bumper).
- 2026-07-13 (cont.): Second playtest round after fixes. Found and fixed two more real bugs: (1) flippers were spaced too close together (120px apart, 90px arms) so they physically overlapped at rest, and rest/active angles were swapped — flippers drooped further down when pressed instead of swinging up; fixed by widening pivot spacing (110/310) and correcting rest=droop-down/active=swing-up angle signs. (2) The drain/respawn logic wrote `position`/`linear_velocity` directly on the ball's RigidBody2D from `_physics_process`, which fights Godot's physics server transform sync — reproduced via a temporary headless debug probe (removed after fix): after one respawn the ball froze permanently with gravity no longer applying, confirmed in a scripted headless trace. Fixed by moving the teleport into `_integrate_forces()`, the correct hook for direct RigidBody2D state changes — reverified via the same headless trace, ball now falls/drains/respawns correctly on repeat. Also wired the previously-inert `launch_ball` input action to an actual impulse, and moved ball spawn off the bumper's dead-center vertical line. Playtested live: flippers now stay pinned, swing the right direction, ball loops indefinitely without getting stuck. Known gap (acceptable for §10a): no outlane/guide walls yet, so a missed flipper just drains — that's expected table-layout work, not core physics feel, and not yet in scope.
- §10a gate status: physics feel (flipper response, ball behavior, respawn loop) confirmed working end-to-end by the designer. Table layout (guides, ramp, additional bumpers) still open before calling §10a fully "done," per GDD — flagged for next session, not blocking further physics tuning.
- 2026-07-12: Reviewed CLAUDE.md + GDD.md, confirmed scope and understanding. Initialized private GitHub repo (github.com/nobody174/pinball-adventure). Installed Godot 4.7 (GDScript, not Mono) via winget. Built first §10a physics-feel prototype: `core/physics/physics_prototype.tscn` with two flippers, ball, boundary walls, one bumper, placeholder shapes only. Keyboard input (Left/Right arrows = flippers, Space = nudge, Up = launch) and touch zones both wired from the start. Validated headless (imports clean, runs without script errors) — not yet playtested for feel by a human. That's the next step.
- 2026-07-13: First real playtest, on-device. Found and fixed real bugs: (1) boundary walls and bumper had collision shapes but no visuals — added Polygon2D sprites so the table is actually visible; (2) flippers were free-floating RigidBody2D with no pivot constraint, so applying torque about their off-center collision shape caused them to translate away from their pivot instead of just rotating — fixed with a PinJoint2D anchoring each flipper to a fixed StaticBody2D at its pivot, plus forcing center_of_mass back to the pivot in `flipper.gd`. Also added: PD-style damping term to the flipper torque controller (pure P-control could ring at high torque — flagged in `docs/inspiration/physics_notes.md`), and a basic nudge impulse on the ball (cooldown-limited, no tilt lockout yet). Added `docs/inspiration/` (pinball_fantasies_analysis.md, physics_notes.md, mission_ideas.md) — principle-level design notes, not derived from original game code/assets, linked from GDD.md. Not yet re-playtested after these fixes — next step.

## How to Playtest (cabin / any machine)
1. `git pull` in the project folder.
2. Open Godot 4.7, "Import" this folder (project.godot), or just double-click project.godot if Godot is already associated.
3. Press Play (F5) — it should launch straight into the physics prototype scene.
4. Controls: Left/Right arrow keys = flippers, Space = nudge, Up = launch ball (not yet wired to a plunger, ball just starts near the top).
5. What to judge: does the flipper response feel snappy or sluggish? Does the ball ever tunnel through a flipper at speed? Does it feel "right" compared to real/other pinball games? Nothing else matters yet — no art, no objectives, no theme.

## Decisions/Deviations from GDD
(none yet — log anything here that extends or diverges from the locked GDD, per CLAUDE.md "Source of Truth")
